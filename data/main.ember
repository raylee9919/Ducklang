s32 a := 19;        // Explicit type declaration.
b := 1 + a + 4;     // Type doesn't have to be explicit in declaration.

//print(a);                       // print() is intrinsic.
//print("value of b is %.", b);   // Formatting string is easy.
//quack("a=%, b=%\n", a, b);      // Since it is a duck lang...
